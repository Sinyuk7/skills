# Schema Pointer

The canonical JSON schemas for this skill live in `references/schemas.md`.

Load `references/schemas.md` whenever you need the exact structure for:

- `evals/evals.json`
- `grading.json`
- `timing.json`
- `benchmark.json`
- related benchmark and history files

Use this file only as a convenience pointer so router-style workflows can reference a `templates/` location without duplicating the source of truth.
