# refine-case

Case action, not a top-level stage.

Use this action when:

- new evidence arrives
- the current findings need correction
- the handoff needs targeted improvements

Typical updates:

- refresh `inventory.yaml`
- extend `evidence-pack.xml`
- revise `findings.xml`
- regenerate `handoff.xml`
