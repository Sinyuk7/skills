# verification-closeout

Purpose:

- record how the chosen resolution was verified
- close out the case with explicit result tracking

Expected outputs:

- `resolve/verification.md`
- updates to `status.yaml`
- optional export artifacts

Rules:

- verification should state what was checked and what remains uncertain
- closure should be explicit, not implied
