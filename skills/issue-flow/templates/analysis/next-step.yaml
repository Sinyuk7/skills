case_id: "{{case_id}}"
timestamp: "{{timestamp}}"

recommended_action: "{{action}}"  # resolve, external_handoff, close_as_non_actionable, blocked

confidence: "{{confidence}}"  # high, medium, low

reasoning: |
  {{reasoning}}

prerequisites:
  - "{{prerequisite}}"
  - "{{prerequisite}}"

notes: |
  {{additional_notes}}
