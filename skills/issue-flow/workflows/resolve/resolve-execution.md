# resolve-execution

Purpose:

- continue from a case handoff into an optional resolution path

Expected outputs:

- `resolve/resolution.xml`
- optional code changes in the user project
- `resolve/commits.yaml`

Rules:

- resolution can be code, escalation, ownership transfer, or documented closure
- prior evidence and handoff artifacts remain part of the case history
