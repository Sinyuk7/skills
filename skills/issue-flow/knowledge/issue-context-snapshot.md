# issue-context snapshot guidance

Input:

- `<project-root>/ISSUE_CONTEXT.md` when present

Output:

- `project-context.snapshot.md`

Rules:

- preserve the source document's useful structure where possible
- do not force a fixed schema
- keep only the context that matters for the current case
