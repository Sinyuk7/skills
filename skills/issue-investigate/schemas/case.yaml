# issue-investigate case.yaml reference

```yaml
case_id: "OMMUSIC-3397323"
created: "2026-04-23T09:00:00Z"
updated: "2026-04-23T09:15:00Z"

status: investigating | blocked | investigated | resolved

summary: "Playback does not recover after audio focus changes"
user_context: |
  播放过程中切换音频焦点后无法恢复播放。

primary_question: "Why did playback fail to recover after the focus change?"
primary_time_anchor: "2026-04-20T14:05:42.333+08:00"
secondary_anchors:
  - "2026-04-20T14:05:42.347+08:00"
named_stakeholders:
  - "徐杉"
  - "李帅文"

next_step:
  action: investigate | blocked | resolve | complete
  note: "Short operator-facing note"

evidence_sources:
  - kind: log
    path: "/tmp/audio-focus-bug/player.log"
    note: "App log from failed repro"
  - kind: screenshot
    path: "/tmp/audio-focus-bug/ui.png"
    note: "UI state at failure"
  - kind: code_reference
    path: "biz/player/src/main/java/com/example/PlayerManager.kt"
    note: "User suspects this area"

evidence_window:
  kind: time_anchor | keyword_search | archive_slice
  anchor: "2026-04-20T14:05:42.333+08:00"
  notes: "Focused on lines surrounding the primary anchor"

root_cause:
  summary: "Focus recovery callback exits before playback state is restored"
  location: "biz/player/.../CarAudioFocusManager.kt:142"
  evidence_refs:
    - "/tmp/audio-focus-bug/player.log:76620"

blocked_reason:
  kind: missing_evidence | ambiguous_anchor | anchor_mismatch
  detail: "Need the log that covers 14:05:42.333"
```

Rules:

- `case.yaml` is the only machine-readable state file in the case directory.
- Unknown keys must be preserved during updates.
- `evidence_sources` entries must be appended or merged; never dropped silently.
- Only one of `root_cause` or `blocked_reason` should be the active terminal reason for the current state.
